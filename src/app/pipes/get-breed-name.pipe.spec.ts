import { GetBreedNamePipe } from './get-breed-name.pipe';

describe('GetBreedNamePipe', () => {
  it('create an instance', () => {
    const pipe = new GetBreedNamePipe();
    expect(pipe).toBeTruthy();
  });
});
